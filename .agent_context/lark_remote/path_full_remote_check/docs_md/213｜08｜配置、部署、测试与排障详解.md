{
  "ok": true,
  "identity": "user",
  "data": {
    "document": {
      "content": "<title>08｜配置、部署、测试与排障详解</title>\n\n<callout emoji=\"✅\">\n**本章目标：** 讲清本地开发需要看哪些配置和命令，以及出问题时如何从日志、API、测试定位。\n</callout>\n\n# 1. 配置加载\n\n```mermaid\nflowchart TD\n  Env[.env and OS env] --> AppConfig[AppConfig]\n  Root[DEER_FLOW_PROJECT_ROOT] --> Paths[Paths]\n  ConfigPath[DEER_FLOW_CONFIG_PATH or config.yaml] --> AppConfig\n  ExtPath[DEER_FLOW_EXTENSIONS_CONFIG_PATH or extensions_config.json] --> Extensions[ExtensionsConfig]\n  AppConfig --> Models[models]\n  AppConfig --> Sandbox[sandbox]\n  AppConfig --> Skills[skills]\n  AppConfig --> Memory[memory]\n  AppConfig --> DB[database/checkpointer/run_events]\n  Extensions --> MCP[mcp_servers]\n  Extensions --> SkillState[skill states]\n```\n\n# 2. Makefile 命令地图\n\n| 命令 | 用途 | 底层入口 |\n|-|-|-|\n| `make setup` | 交互式初始化 | `scripts/setup_wizard.py` |\n| `make doctor` | 诊断配置和系统要求 | `scripts/doctor.py` |\n| `make check` | 检查依赖 | `scripts/check.py` |\n| `make install` | 安装 backend/frontend 依赖 | `uv sync`、`pnpm install` |\n| `make dev` | 本地开发启动 | `scripts/serve.sh --dev` |\n| `make docker-start` | Docker 开发启动 | `scripts/docker.sh start` |\n| `make up` | 生产 Docker 启动 | `scripts/deploy.sh` |\n\n# 3. 部署模式\n\n```mermaid\nflowchart LR\n  Dev[make dev] --> Check[scripts/check.py]\n  Check --> Serve[scripts/serve.sh --dev]\n  Serve --> Gateway[uvicorn Gateway :8001]\n  Serve --> Frontend[pnpm dev :3000]\n  Serve --> Nginx[nginx :2026]\n  DockerDev[make docker-start] --> ComposeDev[docker-compose-dev.yaml]\n  Prod[make up] --> Compose[docker-compose.yaml]\n  Compose --> NginxProd[nginx production]\n  Compose --> GatewayProd[gateway container]\n  Compose --> FrontendProd[frontend container]\n```\n\n# 4. 测试入口\n\n```bash\ncd deer-flow  # 或进入你的本地仓库根目录\nmake check\nmake doctor\nmake detect-blocking-io\n\ncd backend\nmake test\nmake lint\n\ncd ../frontend\npnpm typecheck\npnpm test\npnpm build\npnpm test:e2e\n```\n\n# 5. 排障决策树\n\n```mermaid\nflowchart TD\n  Problem[问题出现] --> Start{服务是否启动?}\n  Start -->|no| Check[make check / make doctor]\n  Start -->|yes| API{API 是否 2xx?}\n  API -->|no 401/403| Auth[检查 auth/CSRF/cookie]\n  API -->|no 5xx| Logs[看 logs/gateway.log]\n  API -->|yes| UI{UI 是否更新?}\n  UI -->|no| FE[检查 useThreadStream callbacks / React Query]\n  UI -->|yes| Tool{工具/文件异常?}\n  Tool -->|yes| Sandbox[检查 sandbox_id、virtual path、uploads/artifacts]\n  Tool -->|no| Done[定位到具体模块测试]\n```\n\n# 6. 日志与快速 API\n\n```bash\ncurl http://localhost:2026/api/models\ncurl http://localhost:2026/api/skills\ncurl http://localhost:2026/api/memory/status\ncurl http://localhost:2026/api/mcp/config\n\nls -la logs\ntail -f logs/gateway.log\n```\n\n---\n\n# 补充：设计取舍、重点代码与阅读路径\n\n<callout emoji=\"💡\">\n**设计目的：**把配置、部署、测试统一成 Makefile/scripts，是为了给开发者稳定入口。\n</callout>\n\n| 维度 | 说明 |\n|-|-|\n| 收益 | 收益是命令统一；代价是脚本层会隐藏底层服务细节。 |\n| 代价 | 见收益说明中的代价部分；此章节采用收益与代价合并描述。 |\n| 重点代码 | 重点代码：`Makefile`、`scripts/serve.sh`、`scripts/check.py`。 |\n| 阅读路径 | 阅读路径：先看 make 目标，再跳到脚本实现。 |\n\n```mermaid\nflowchart TD\n  A[设计目的] --> B[模块职责]\n  B --> C[收益]\n  B --> D[代价]\n  C --> E[重点代码]\n  D --> E\n  E --> F[阅读路径]\n```",
      "document_id": "XyAFdSxNooUswXxcVx8mbR7iyqg",
      "revision_id": 19
    }
  }
}
